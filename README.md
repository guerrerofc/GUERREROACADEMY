# Here are your Instructions

## GitHub Pages

GitHub Pages is configured to deploy from the `/docs` folder on the `main` branch.

To update the site, modify the files inside `docs/`. The `docs/.nojekyll` file disables Jekyll processing so the HTML/CSS/JS files are served as-is without any build pipeline.
